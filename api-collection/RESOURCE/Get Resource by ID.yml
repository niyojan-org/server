info:
  name: Get Resource by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{resource_url}}/{{resourceId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Fetch a single resource by its ID
  
  Returns full resource details including file URL and metadata
