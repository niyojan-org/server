info:
  name: Update won data
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{user_route}}/me"
  body:
    type: json
    data: |-
      {
        "name" : "Abhi"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
