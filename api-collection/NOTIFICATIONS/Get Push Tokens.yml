info:
  name: Get Push Tokens
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/notifications/push-tokens"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Get all active push tokens for the authenticated user
