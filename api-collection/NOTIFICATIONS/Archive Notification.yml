info:
  name: Archive Notification
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/notifications/:notificationId/archive"
  params:
    - name: notificationId
      value: 548e7a35-4136-4fe1-b823-49d4461f743d
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Archive a notification (removes it from the user's inbox)

  Path Parameters:
  - notificationId: UUID of the notification to archive
