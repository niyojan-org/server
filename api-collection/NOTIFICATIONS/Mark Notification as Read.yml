info:
  name: Mark Notification as Read
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/notifications/read"
  body:
    type: json
    data: |-
      {
        "notificationIds": ["548e7a35-4136-4fe1-b823-49d4461f743d"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Mark a specific notification as read

  Body Parameters:
  - notificationId: UUID of the notification to mark as read
