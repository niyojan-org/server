info:
  name: GET ORGANIZATIONS SUMMARY
  type: http
  seq: 10

http:
  method: GET
  url: "{{org_tm_url}}/summart"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
