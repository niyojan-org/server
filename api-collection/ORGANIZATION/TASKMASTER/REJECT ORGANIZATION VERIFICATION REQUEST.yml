info:
  name: REJECT ORGANIZATION VERIFICATION REQUEST
  type: http
  seq: 3

http:
  method: POST
  url: "{{org_tm_url}}/:id/reject"
  params:
    - name: id
      value: 6972ffb802570490dc685142
      type: path
  body:
    type: json
    data: |-
      {
        "reason": "Information is not correct"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
