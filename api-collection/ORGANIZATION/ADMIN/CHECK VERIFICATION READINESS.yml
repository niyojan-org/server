info:
  name: CHECK VERIFICATION READINESS
  type: http
  seq: 3

http:
  method: GET
  url: "{{org_url}}/admin/verification/check"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
