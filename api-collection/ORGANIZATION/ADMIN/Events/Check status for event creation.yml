info:
  name: Check status for event creation
  type: http
  seq: 1

http:
  method: GET
  url: "{{org_admin_url}}/events/creation-status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
