info:
  name: RAISE ORGANIZATION VERIFICATION
  type: http
  seq: 4

http:
  method: POST
  url: "{{org_url}}/admin/verification/raise"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
