info:
  name: GET ORGANIZATION INFO
  type: http
  seq: 3

http:
  method: GET
  url: "{{org_url}}/admin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
