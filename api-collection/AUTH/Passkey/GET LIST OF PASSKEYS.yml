info:
  name: GET LIST OF PASSKEYS
  type: http
  seq: 1

http:
  method: GET
  url: "{{auth_url}}/passkeys/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
