info:
  name: Verify Email
  type: http
  seq: 3

http:
  method: POST
  url: "{{auth_url}}/verify-email"
  body:
    type: json
    data: |-
      {
        "email" : "iamabhi1906@gmail.com",
        "token" : "kdfk;lmdflknlkdfm l;kdff"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
