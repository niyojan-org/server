info:
  name: SETUP TOTP
  type: http
  seq: 1

http:
  method: POST
  url: "{{auth_url}}/totp/setup"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
