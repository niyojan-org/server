info:
  name: Forget Password
  type: http
  seq: 5

http:
  method: POST
  url: "{{auth_url}}/forgot-password"
  body:
    type: json
    data: |-
      {
        "email" : "iamabhi061@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
