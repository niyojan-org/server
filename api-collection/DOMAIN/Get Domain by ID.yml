info:
  name: Get Domain by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/domains/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
